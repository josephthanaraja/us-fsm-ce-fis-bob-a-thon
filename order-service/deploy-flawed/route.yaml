apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  to:
    kind: Service
    name: order-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow

# Made with Bob
