# MCP Server Dependencies
# 
# This file lists all Python packages required to run the server.
# Install with: pip install -r requirements.txt
#
# Key Concepts:
# - requirements.txt is the standard way to document Python dependencies
# - Version pinning (==) ensures reproducible installations
# - Comments help explain why each package is needed

# FastMCP - The MCP server framework
fastmcp>=0.1.0

# Starlette - Web framework (used by FastMCP for HTTP routes)
starlette>=0.27.0

# Uvicorn - ASGI server (used to run the server)
uvicorn>=0.23.0